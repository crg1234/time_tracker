 <div class="container-fluid">

  <div class="row">
    <%= render 'shared/sidebarshowpages'%>
    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4"><div class="chartjs-size-monitor"><div class="chartjs-size-monitor-expand"><div class=""></div></div><div class="chartjs-size-monitor-shrink"><div class=""></div></div></div>
      <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Clients</h1>
        <div class=" mb-2 mb-md-0">
        <%= link_to 'Edit', edit_client_path(@client), class: 'btn btn-primary mr-4' %>
        <%= link_to  'Delete', client_path(@client),  data: { turbo_method: :delete, turbo_confirm: 'Are you sure you want to delete this client?' }, class: 'btn btn-danger' %>

        <%# <%= link_to new_client_path, class: "create-client-link" do %>
        <%# <span class="plus-symbol">➕</span> %>
        <%# <% end %> 
          <%# <div class="btn-group me-2">
            <button type="button" class="btn btn-sm btn-outline-secondary">Share</button>
            <button type="button" class="btn btn-sm btn-outline-secondary">Export</button>
          </div>
          <button type="button" class="btn btn-sm btn-outline-secondary dropdown-toggle">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-calendar" aria-hidden="true"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
            This week
          </button> %>
        </div>
      </div>
    <h1></h1>

    <h2><b>Company: </b><%= @client.company_name %></h2>
      <p><b>Full name: </b>  <%= @client.last_name %></p>
      <p><b>Email: </b><%= @client.email %></p>
      <p><b>Phone number: </b><%= @client.phone_number %></p>
      <p><b>Billing address: </b><%= @client.billing_address %></p>
      <button type="button" class="btn btn-light" data-bs-toggle="modal" data-bs-target="#newProjectPopUp">Add Project</button>

      </main>
  </div>


<div class="modal fade" id="newProjectPopUp" tabindex="-1" aria-labelledby="newProjectPopUpLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="exampleModalLabel">New Project</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="rounded p-1 w-100">
            <%= simple_form_for [@client, @project] do |f| %>
              <%= f.input :name %>
              <%= f.input :deadline %>
              <%= f.submit class: 'btn btn-success' %>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
